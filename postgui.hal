# ============================================================================
# postgui.hal — HAL connections that require the GUI to be loaded
#
# CURRENTLY DISABLED: The compound-slide HAL component is not yet integrated
# into the deployed GUI. These connections will fail if uncommented because
# the "compound-slide" pins don't exist yet.
#
# For now, MPG jog-counts are connected directly in the main HAL file.
# When the compound-slide feature is integrated into the GUI, uncomment
# these lines and remove the direct jog-counts connections from industry-cam.hal.
# ============================================================================

# --- DISABLED until compound-slide component is integrated into GUI ---
# net mpg-x-counts => compound-slide.mpg-x-in
# net mpg-z-counts => compound-slide.mpg-z-in
# net mpg-x-scale => compound-slide.jog-scale
# net x-jog-final  compound-slide.x-jog-counts => joint.0.jog-counts axis.x.jog-counts
# net z-jog-final  compound-slide.z-jog-counts => joint.1.jog-counts axis.z.jog-counts
